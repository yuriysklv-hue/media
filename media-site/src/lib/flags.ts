// Флаги видимости временно скрытых блоков.
// Пока у медиа нет соцканалов (Telegram/VK) и рабочего сервиса рассылки,
// соответствующие кнопки/формы — пустые заглушки. Скрываем их из вида,
// НЕ удаляя разметку и стили: возврат = смена флага false → true.
//
// Что вернуть при включении:
//   SHOW_SOCIALS   → прописать реальные URL каналов TG/VK в Footer.astro
//                    (заодно закрывает sameAs/twitter:site из SEO-задачи 7-B);
//   SHOW_NEWSLETTER→ подключить рабочий провайдер формы (NEXT_SESSION.md, задача 6).
//
// RSS в футере скрытием НЕ управляется — канал /rss.xml рабочий, ссылка живая.

/** Ссылки на собственные соцканалы (TG/VK). Появятся — включить. */
export const SHOW_SOCIALS = false;

/** Форма подписки и CTA «Подписаться». Появится сервис рассылки — включить. */
export const SHOW_NEWSLETTER = false;
