# Медиа об adtech и маркетинге — контекст для Claude

> **Название и домен: `1screen.ru`** (обыгрывает «первый экран»). Домен **зарегистрирован** (reg.ru, 06.07.2026), сайт задеплоен на **Timeweb Cloud** и публично открыт на `https://1screen.ru` — см. раздел «Статус запуска» ниже. Прежнее рабочее название «Охват» — заглушка, больше не используется.
> Дескриптор бренда: **«Медиа о рекламе и маркетинге»** (идёт «расширением» логотипа в шапке и футере).
> Репозиторий документации: `github.com/yuriysklv-hue/media`.

---

## Что это за проект

Профессиональное медиа о **рекламе, рекламных технологиях и маркетинге** для России и СНГ. Гибридная модель:
- **ИИ-агенты** пишут новости, дайджесты, обзоры (объём и скорость);
- **эксперты-люди** пишут колонки, аналитику и флагманские отчёты (глубина).

Аудитория двухуровневая: практики (таргетологи, медиабаеры, аналитики) и управленцы (CMO, CEO). Референсы качества: Digiday, The Drum, Detailed.com. Анти-референс: VC.ru.

Полное позиционирование — в `00_Позиционирование_медиа.md`.

---

## Репозитории (план)

| Репо | Назначение | Стек |
|---|---|---|
| `media` (этот) | ТЗ-документация + макеты **+ сайт в подпапке `media-site/`** | Markdown + Astro |
| `media-site` | Сайт — **инициализирован как подпапка `media-site/` в этом репо** (пока не отдельный репозиторий; вынести позже) | Astro + Tailwind + Content Collections → Timeweb Cloud |
| `media-agents` | Пайплайн агентов — **код готов и работает в проде** (09.07.2026 опубликован первый боевой батч). Собирает RSS → фильтр → перевод → написание → QA → **PR в этот репо** (`media-site/src/content/news/`). Контекст пайплайна — в его собственном `CLAUDE.md`. В новой сессии подключать командой «добавь репозиторий media-agents» (add_repo) | Python (GLM/z.ai + DeepSeek, GitHub Actions по cron) |

> ⚠️ **Почему подпапка, а не отдельный репо:** сайт живёт в `media-site/` внутри `media`, потому что рабочие сессии имеют доступ только к репозиторию `media`. Timeweb деплоит подпапку через настройку **«Путь до директории проекта» = `media-site`**. По ТЗ сайт со временем выносится в отдельный репозиторий — история сборки к тому моменту не понадобится.

---

## Документы ТЗ (в этом репо)

| Файл | О чём |
|---|---|
| `00_Позиционирование_медиа.md` | Идея, аудитория, ниша, тональность, принцип «анти-ИИ-голос» |
| `01_Контент-стратегия.md` | Рубрики, форматы, фронт-маттер, ритм публикаций |
| `02_Структура_и_дизайн_сайта.md` | Информационная архитектура, типы страниц, дизайн-система |
| `03_Архитектура_агентов.md` | Конвейер агентов, роли, LLM-стратегия (GLM/DeepSeek/Claude) |
| `04_Тех_стек_и_интеграции.md` | Astro, источники данных, оркестрация |
| `05_План_запуска.md` | Roadmap: этапы 0–3, MVP-2, релизы 3–4 |
| `06_SEO_GEO_и_AI-разметка.md` | SEO, schema.org, `llms.txt`, политика по AI-ботам |
| `07_План_миграции_бота.md` | Перенос старого adtech-парсера в `media-agents` |
| `08_Итерации_разработки.md` | Пошаговый план для Claude Code (итерации 0–7) |
| `09_Итерация_1_каркас_сайта.md` | Детальная спец каркаса сайта (писалась под старый газетный макет — сверять с этим CLAUDE.md, канон = минимал) |
| `NEXT_SESSION.md` | **Задача на следующую сессию** — что делать дальше в `media-site` (Итерация 3) |
| `CONTENT_TODO.md` | **Чек-лист замены демо-контента** реальными материалами + статичные тексты |

---

## Макеты сайта (`mockups/`)

`mockups/1screen-mockup-minimal.html` — **актуальный, канонический макет.** Направление «тех/продукт минимал»: гротеск (sans), чистый белый фон, много воздуха, контент text-only. Интерактивный HTML-макет двух ключевых страниц (**главная** + **страница материала**), переключаются в служебной панели вверху. Самодостаточный файл, светлая/тёмная тема, брендирован под `1screen.ru`, тексты по принципу «анти-ИИ-голос». **Эту вёрстку и стиль сохраняем** — на них строится `media-site` (Итерация 1).

`mockups/1screen-mockup.html` — ранняя «газетная» версия (serif-forward, тёплая off-white «бумага», плотные линейки, буквица). Оставлена как история: направление **отклонено** — читалось как broadsheet-газета (Ведомости), а не как медиа о рекламе. Ориентироваться на минимал.

`mockups/site-mockup.html` — самая ранняя версия с рабочим названием «Охват». История; брендинг и часть текстов устарели.

### Зафиксированные дизайн-решения (сессия 03.07.2026, направление сменено 05.07.2026)

Направление — «тех/продукт минимал»: чистая продуктовая эстетика по референсу **Detailed.com** (sans, белый фон, воздух, текст-форвард) с дисциплиной в цвете от трейд-пресс (**Digiday / The Drum**). Сознательно ушли от газетного broadsheet-сигнала (serif + тёплая «бумага»), который делал сайт похожим на Ведомости. Итог:

- **Палитра «Петроль» на холодном белом.** Один акцент — бирюзово-синий `#14636B` (в макете чуть ярче `#0F6F78` для белого фона, `#0A5A62` для текстовых ссылок — контраст). Нейтралы холодные: чистый белый фон `#FFFFFF`, поднятые поверхности `#F5F7F8`, чернила `#10141B`, линейки `#E7EAEE`. Тёмная тема — почти-чёрный `#0B0D11` + яркий бирюзовый акцент. (Тёплая «бумага» газетного варианта отклонена — читалась как newsprint.)
- **Один акцент, без второго цвета.** Использовался переключатель на 5 палитр (Петроль / Индиго / Хвоя / Слива / Охра) для выбора — выбран Петроль, переключатель убран.
- **Контент текстовый, без картинок.** У новостей на главной нет превью-обложек — только текст (заголовок, лид, мета). В продуктовом минимале это осознанная позиция («не разбавляем стоками»), а не «лента информагентства». Картинка допустима **опционально только для главного материала (feature)**, сейчас отключена.
- **Sans-forward типографика (гротеск).** Заголовки, лиды, тело, подписи — всё sans; serif не используется. Крупные веса (750–800) и плотный трекинг в заголовках; mono (`ui-monospace`) — в мете, датах, цифрах, тегах (продуктовая текстура). В макете системный sans-стек как стенд-ин; на проде — **Inter** (или близкий гротеск).
- **Категории без цвета.** Раздел 7 ТЗ 02 предлагал цветовую кодировку категорий по гео (4 цвета) — **отклонено**: слишком пёстро. Категории — типографские подписи капсом, гео — маленький нейтральный тег `РФ / МИР / АЗИЯ`.
- **Логотип-локап.** Sans-вордмарк `1screen` (цифра «1» — акцентным Петролем) + `.ru` мелкой мéтой, рядом через тонкую линейку дескриптор `Медиа о рекламе и маркетинге` капсом. Дескриптор скрывается в тесной зоне (901–1120px) и на очень узких экранах; вордмарк — всегда. (В газетном варианте вордмарк был serif — теперь sans.)
- **Продуктовые детали.** Акцентные тик-маркеры перед подписями секций (ритм бренд-цвета вместо картинок); hover-стрелка «→» у новостей; залипающий правый сайд-бар; анимированное подчёркивание навигации; тень шапки при скролле; прогресс-бар чтения на материале; пульс «live»-точки. Сдержанно, с учётом `prefers-reduced-motion`.
- **Тексты — по принципу «анти-ИИ-голос».** Вся копия в макете вычищена от ИИ-клише (конструкция «не X, а Y», слово «вердикт», трансформационные зачины «перестало быть… стало», «X ради X»). Оговорки и хеджирование («по данным самого…», «пока рано судить») — сохраняем. Канон — раздел 7 в `00_Позиционирование_медиа.md`.
- **Структура главной:** hero (текстовый) + «Главное сейчас» → две колонки: лента «Свежее» + залипающий правый сайд-бар («Дайджест недели», «Популярное» с рейтингом 1–5) → «Мнения» → подписка → футер.
- **Структура материала:** хлебные крошки → категория → H1 → лид (крупный sans, без буквицы) → мета с автором → тело (колонка ~680px) со спец-блоками (callout, statblock, сравнительная таблица, источник, теги) → карточка автора → «Читайте также» → подписка. Сверху — прогресс-бар чтения.

> ⚠️ Отклонения от ТЗ 02 (приоритетнее исходного ТЗ): акцент `#14636B` вместо red-orange `#C8472B`; **sans-forward вместо serif** (ТЗ 02 предлагал Source Serif Pro для заголовков/тела — отклонено, всё на гротеске Inter); **холодный белый фон вместо тёплого off-white**; нет цветокодировки категорий; контент без превью-картинок. При обновлении `02_Структура_и_дизайн_сайта.md` перенести туда (краткая пометка уже добавлена в шапку ТЗ 02).

---

## Статус запуска (сессии 06.07.2026)

Этап 0 закрыт, Итерации 1–2 закрыты: инфраструктура, главная, контент-модель, страница материала, ленты рубрик и SEO-каркас — в продакшне.

**Домен.** `1screen.ru` зарегистрирован на **reg.ru**. История проверена (Wayback + `site:` в поиске): в 2018 стоял заброшенный дефолтный Drupal, спам-прошлого нет — чистый. DNS-серверы reg.ru (`ns1.reg.ru` / `ns2.reg.ru`).

**Сайт (`media-site/`).** Стек **Astro 5 + Tailwind 4 + Inter** (self-hosted через Fontsource `@fontsource-variable/inter`, без CDN) + **@astrojs/mdx@^4** и **@astrojs/sitemap@^3** (mdx 7.x требует Astro 7 — не обновлять). Дизайн — перенос канонического макета `mockups/1screen-mockup-minimal.html` (палитра «Петроль», sans, text-only), вся дизайн-система в `src/styles/global.css` (home + article).

**Итерация 2 (сделано 06.07.2026, вечер):**
- **Content Collections** `src/content.config.ts`: `news`, `digest`, `reviews`, `columns`, `reports` (общая схема: title, description ≤160, pubDate, category, geo[] `РФ|МИР|АЗИЯ`, tags[], author→reference, featured, source, readingTime, highlights) + `authors`. Тип материала = имя коллекции, во фронт-маттере не дублируется.
- **Страница материала** `pages/article/[...slug].astro` — единый маршрут `/article/<slug>/` для всех коллекций: крошки, eyebrow, лид, мета с автором и шарингом (TG/VK/копировать — рабочие), спец-блоки, source-box и теги из фронт-маттера, карточка автора, «Читайте также», прогресс-бар чтения (проп `reading` у BaseLayout). JSON-LD `NewsArticle` + `og:type=article`.
- **MDX-компоненты** `src/components/article/`: `Callout`, `StatBlock`, `CmpTable` — передаются через `<Content components={...}>`, импорты в контенте не нужны.
- **Ленты рубрик** `/news` `/digest` `/reviews` `/columns` `/reports` (`layouts/SectionPage.astro` + `components/ArticleListItem.astro`); навигация шапки/футера провязана.
- **Главная динамическая** (`getCollection`): hero = свежий `featured`, «Главное сейчас» = 3 новости (live-точка младше 3 ч), «Свежее» = 6 news+reviews, дайджест из `highlights`, «Популярное» = 5 свежих (без статистики пока), «Мнения» = 2 колонки.
- **SEO/AI-слой (ТЗ 06 закрыт в коде):** `sitemap-index.xml`; `robots.txt` (LLM-боты Allow, Diffbot/ImagesiftBot Disallow); **`/llms.txt` + `/llms-full.txt`** (динамические, из коллекций); **RSS `/rss.xml`**; JSON-LD-граф `NewsMediaOrganization`+`WebSite` на всех страницах, `NewsArticle` (+speakable/keywords/articleSection) и `BreadcrumbList` на материалах, `CollectionPage`+`BreadcrumbList` на рубриках; og:image `/og-default.png` 1200×630 + `logo.png` 512 (сгенерированы Playwright'ом); meta robots, hreflang. Вне кода (владелец): Вебмастер/GSC/Метрика.
- **Контент:** 13 демо-материалов + автор `y-sokolov`. **Всё демо — план замены на реальные в `CONTENT_TODO.md`** (3–4 дня).
- Хелперы: `src/lib/format.ts` (даты по Europe/Moscow — на сборке TZ=UTC, иначе время врёт), `src/lib/sections.ts` (справочник рубрик). Tailwind preflight сбрасывает `list-style` — для `.article-body` задан явно.

**Контракт с `media-agents` (сделано 07.07.2026):** сайт принимает контент агентского пайплайна как есть.
- **Категории — машинные слаги** из `config/vocabulary.yaml` агентов: `adtech-ru | adtech-world | adtech-asia | market-news | tools | creative` + сайтовый `editorial` (только для ручных материалов). В схеме — `z.enum`, подписи для бейджей/RSS/schema.org — в `src/lib/categories.ts` (`formatCategory`); все три adtech-слага показываются как «Adtech» — гео различает тег РФ/Мир/Азия. Демо-контент переведён на слаги.
- **Авторы-службы** в `src/content/authors/`: `news-world` («Служба новостей Мир»), `news-ru`, `news-asia`. У авторов новое поле `team: true` — в JSON-LD такой автор размечается `Organization`, а не `Person`.
- **Поля контракта в схеме статьи** (snake_case, как в ТЗ пайплайна): `social_title` (≤100, генерирует Enricher), `sources_count` и `week` (`YYYY-Wnn`, для digest).

**Деплой (Timeweb Cloud, App Platform).** Тип приложения **«Другой»** (Node.js 24), автосборка из GitHub-репо `yuriysklv-hue/media`, ветка `main`. Настройки сборки: команда `npm ci && npm run build`, «Путь до директории проекта» = `media-site`, «Директория сборки» = `dist`, поле «Зависимости» (системные apt-пакеты) — **пусто** (npm уже в образе Node). Автодеплой при пуше в `main`. Публичный сайт `https://1screen.ru` (авто-SSL Let's Encrypt от Timeweb). Тех-адрес приложения: `*.twc1.net`.

> ⚠️ **Сбои сборки Timeweb:** деплой изредка падает с `i/o timeout` / `Could not resolve host: github.com` в логе — это их билд-инфраструктура, код ни при чём. Лечится кнопкой ▶ (перезапуск деплоя) через несколько минут (проверено 06.07.2026). План: перевести доступ к репо на SSH и закрыть репозиторий — см. `NEXT_SESSION.md`, задача 2.

> ⚠️ **Почему Timeweb, а не Vercel:** Vercel в РФ режется на уровне ТСПУ/РКН — сайт не открывался без VPN (проверено 06.07.2026, с VPN работал, без — нет). Для медиа с аудиторией РФ это блокер, поэтому переехали на российский **Timeweb Cloud** (открывается в РФ напрямую). Домен, репозиторий и код Astro **не менялись** — только площадка. Старый проект на **Vercel выведен из использования** (домен отвязан). Cloudflare/Netlify/GitHub Pages не рассматривать — в РФ тоже режутся.

**DNS в reg.ru (управление зоной; NS остаются reg.ru — Timeweb «Способ 2: A-запись», без переноса NS):**
```
A   @    →  92.246.76.92     (Timeweb Cloud App Platform)
A   www  →  92.246.76.92     (после привязки www к приложению; ранее — Vercel 216.198.79.1)
```

**Рабочее окружение сессий:** Node 22, npm. Сборка/проверка: `cd media-site && npm run build`. `dig` в образе нет (DNS наружу из песочницы заблокирован — проверять домен со стороны пользователя). Playwright — глобально в `/opt/node22/lib/node_modules` (astro preview + скриншоты).

---

## Агенты запущены (09.07.2026)

Пайплайн `media-agents` **в проде**: секреты настроены, первый боевой батч — **9 реальных новостей — опубликован** (PR смёржен в `main`, статьи в `media-site/src/content/news/`). Плановые прогоны по cron: новости 09:00/21:00 МСК, дайджест вс 21:00 МСК; каждый создаёт PR в этот репо, публикация — ручным merge. Контекст и открытые задачи пайплайна (качество текстов / «анти-ИИ-голос», корпус good/bad, дедуп-дубли) — в `CLAUDE.md` и `NEXT_SESSION.md` репозитория `media-agents`.

> Реальные материалы агентов постепенно вытесняют демо-контент — синхронизировать с `CONTENT_TODO.md`.

### Сессия обратной связи (09.07.2026, PR #12)

Правки сайта по разбору первого батча владельцем (парная часть — в пайплайне `media-agents`):
- **Юридические сноски Meta/Instagram/Facebook** проставлены во всех реальных статьях с их упоминанием (маркер `*` + `<small>* … признана экстремистской и запрещена в РФ</small>` в конце тела). Конвенция взята из бывшего демо `meta-dolya`. На будущее сноску ставит пайплайн (`legal.py`), сайт рендерит её как есть.
- **Обрезанные дескрипшены** у `genprokurory` и `adobe` доведены до законченной фразы (лид = `description`, обрыв «…необос…» недопустим). На будущее чинит enricher.
- **Удалены 8 демо-новостей** (выдуманные факты) — новостная лента только из реальных материалов. Демо-hero (отчёт), 2 колонки и дайджест **оставлены плейсхолдерами**, иначе пустеют hero / «Мнения» / «Дайджест» / «Обзоры» на главной. См. `CONTENT_TODO.md`.
- **Страницы авторов** `pages/authors/[slug].astro` — карточка + все материалы автора + JSON-LD (`Organization` для служб `team:true`, `Person` для людей). Закрывает часть задачи 4 из `NEXT_SESSION.md`; ссылка «Все материалы автора →» больше не `#`.
- **Кнопка шаринга** — глиф «↗» → текстовая метка «Ссылка» + тултип (функция копирования та же).

### Сессия 10.07.2026 (служебные страницы, гео, бургер)

Закрыты задачи 3 и 4 Итерации 3 + гео-ленты. Каждая правка мёржилась отдельным PR (в репо настроен авто-мёрж ветки в `main`).

- **Замена дефолтной статьи.** Демо-отчёт `reklama-v-geoservisah-2026` (единственный в `reports`, был hero-плейсхолдером) удалён; вместо него — колонка `columns/agentic-commerce-a16z-reklama` (автор `y-sokolov`, категория `adtech-ru`, `featured:true` → hero на главной; текст владельца про agentic commerce / a16z, отредактирован под сайт). ⚠️ Коллекция `reports` теперь **пуста** — `/reports` показывает штатную заглушку, при сборке идёт безобидный `WARN glob-loader … "reports" is empty` (не ошибка, деплой проходит); норма вернётся с первым реальным отчётом.
- **Гео-страницы.** `/geo` (хаб) + `/geo/russia`, `/geo/world`, `/geo/asia` — `layouts/GeoPage.astro` фильтрует все коллекции по `geo` (РФ/МИР/АЗИЯ), с `CollectionPage`+`BreadcrumbList`. Футер «Гео»: заголовок-ссылка на `/geo`, пункты провязаны.
- **Служебные страницы.** `layouts/PageLayout.astro` (крошки + H1 + лид + `.article-body`, `BreadcrumbList`). Созданы `/about`, `/editorial`, `/advertising`, `/contacts`; в футере «Медиа» все ссылки провязаны. Контакт на `/contacts` и `/advertising` — **личный email `yuriysklv@gmail.com` (временно)**, заменить на брендовый при появлении почты на домене.
- **Бургер-меню.** Панель `.mobile-menu` под шапкой на <900px (рубрики + гео-пилюли + «О нас» + CTA). Иконка → «×»; закрытие по бургеру/тапу вне/ссылке/Esc/ресайзу; фон не прокручивается; `aria-expanded`/`aria-controls`; ванильный скрипт в `Header.astro`.
- **Копии/атрибуты.** Дескриптор в футере → «Профессиональное медиа о рекламе, рекламных технологиях и маркетинге в Мире и России для профессионалов рынка.»; роль `y-sokolov` → «Руководитель коммерческих продуктов 2ГИС»; на `/about` убрано предложение про трейд-прессу.
- **Бэклог:** заведена задача 6 в `NEXT_SESSION.md` — **подписка на рассылку** (форма-заглушка не работает и стоит непоследовательно; нужен внешний сервис + согласие 152-ФЗ + единый компонент + матрица размещения).

### Сессия 11.07.2026 (подключение поисковых консолей и Метрики)

Закрыта основная часть задачи 7-A из `NEXT_SESSION.md` (подключение внешних панелей). Всё — в `BaseLayout.astro`, значит на всех страницах.

- **Верификация вебмастеров.** В `<head>` добавлены мета-теги подтверждения прав: `google-site-verification` (Google Search Console), `msvalidate.01` (Bing Webmaster Tools) и `yandex-verification` (Яндекс.Вебмастер, добавлен 12.07.2026). Права во всех трёх консолях подтверждены.
- **Яндекс.Метрика.** Счётчик `110606034` подключён штатным кодом Яндекса с опциями `webvisor/clickmap/trackLinks/accurateTrackBounce` + `ecommerce:"dataLayer"` + `<noscript>`-пиксель; данные принимаются. ⚠️ Инлайновый `<script>` обязательно с директивой **`is:inline`** — иначе Astro его бандлит/хойстит и счётчик не инициализируется (та же оговорка для любых сторонних инлайн-скриптов). Это единственный счётчик — Plausible/Umami/GA не ставим.
- **Sitemap.** `sitemap-index.xml` отправлен во все три панели (Google, Bing, Яндекс) 12.07.2026 — индексный файл, поисковики сами доходят до дочернего `sitemap-0.xml` (42 URL).
- **Валидация разметки (12.07.2026).** PageSpeed Insights (моб.): Performance 99, a11y 93, Best Practices 96, SEO 100. schema.org Validator: главная — «нет ошибок, нет предупреждений» (граф `WebSite`+`NewsMediaOrganization`); страница материала — `NewsArticle`, `BreadcrumbList`, `WebSite` все три чистые. Google Rich Results Test: главная — «Организация, 1 элемент без ошибок». **Задача 7-A закрыта полностью.** Замечания по a11y (из-за них 93) исправлены в тот же день — см. блок ниже.

### Сессия 12.07.2026 (a11y — задача 7-G)

Исправлены оба замечания PageSpeed из раздела «Специальные возможности» (были 93). Правки «под капотом», дизайн-канон сохранён (проверено скриншотами + программной проверкой heading-order на 16 страницах).

- **Контраст (G1).** Приглушённый текст завязан на один токен `--ink-3` (`src/styles/global.css`); он давал ≈3.3:1 на белом — ниже WCAG AA (4.5:1). Затемнён: светлая тема `#868f9e`→`#636d7d` (5.23:1 на `#fff`, 4.87 на `#f5f7f8`), тёмная `#6e7789`→`#828b9c` (5.67:1). Одно изменение чинит все подписи (даты, гео-теги, мета, крошки, футер, `.nl-note`). `--ink-2`/`--accent-strong` не трогали (проходят).
- **Порядок заголовков (G2).** Приведено к строгой иерархии h1→h2→h3 без пропусков: на главной «Главное сейчас» `<div>`→`<h2>` и футерные колонки `<h4>`→`<h2>`; на материале карточки «Читайте также» `<h4>`→`<h3>`; на рубриках и гео заголовок страницы `<h2>`→`<h1>` (**раньше у этих страниц вообще не было h1** — заодно плюс к SEO). Лента `ArticleListItem` получила проп `headingLevel` (3 под секцией-h2 на главной, 2 под h1 страницы рубрики/гео). CSS-селекторы обобщены до `:is(h1,h2)`/`:is(h2,h3)` — специфичность та же, вид не изменился.
- **Осталось (владелец):** перепрогнать PageSpeed после деплоя, ждём a11y ≥ 95–100.

### Ревью вёрстки/контента 12.07.2026 (задачи заведены, код не менялся)

Владелец прошёлся по сайту — часть пунктов оказалась вопросами «как устроено» (ответы записаны), часть — задачами. Всё разложено в `NEXT_SESSION.md` с приоритетами. Кратко:
- **8.1 (ВЫСОКИЙ)** — ✅ **сделано 14.07.2026** (см. блок «Сессия 14.07.2026 (подсветка ссылок…)» ниже): markdown-ссылки в теле статьи не выделялись; базовый стиль расширен на все `.article-body a`.
- **7-H (СРЕДНИЙ-ВЫСОКИЙ)** — ✅ **сделано 14.07.2026** (см. блок «Сессия 14.07.2026 (страницы тегов…)» ниже): страницы тегов `/tag/[slug]` + хаб `/tags`, транслит-слаги, «индекс с порогом» ≥3, CollectionPage+Breadcrumb, sitemap-фильтр.
- **7-E (СРЕДНИЙ)** — цитаты инлайновые, не размечены (пример: статья IAB). Решение: сперва спроектировать (инлайн `<q>`/`Quotation`, шаблон vs пайплайн), потом делать.
- **8.2 (НИЗКИЙ)** — пагинация: гипотеза «материалы теряются» не подтвердилась (рубрики/гео выводят всё), это вопрос масштаба — отложено до ~30–50 материалов в рубрике.
- **8.3 / 8.4 (справка)** — дайджест недели (тизер из `highlights`, синхронизируется через коллекцию `digest` при мерже PR) и логика «Главное сейчас» (3 свежих новости) vs «Свежее» (следующие news+reviews) работают by design.
- **IndexNow (7-F)** — отложен до роста потока статей (источники Россия/Азия в `media-agents`).

### Сессия 14.07.2026 (подсветка ссылок в теле — задача 8.1)

Закрыта задача 8.1 из `NEXT_SESSION.md` (быстрая CSS-правка, приоритет ВЫСОКИЙ). Markdown-ссылки `[текст](url)` в теле статьи рендерились как `<a>` без класса `.inline`, а стиль был завязан только на `.article-body a.inline` — ссылки были неотличимы от текста.

- **Фикс** (`src/styles/global.css`): базовый стиль расширен на `.article-body a, .article-body a.inline` (акцент `--accent-strong` + подчёркивание `text-underline-offset:2px`, hover → `--accent`). Тег-пилюлям `.tag-list a` явно добавлен `text-decoration:none` (они не задавали decoration и иначе получали лишнее подчёркивание от базы); `.source-box a` не тронут — уже даёт тот же вид. Проверено скриншотом + `getComputedStyle` на реальной статье.

### Сессия 14.07.2026 (страницы тегов — задача 7-H)

Закрыта задача 7-H из `NEXT_SESSION.md` (приоритет СРЕДНИЙ-ВЫСОКИЙ). Теги под материалами были кликабельны, но вели в `#` — теперь у каждого тега своя лента + хаб.

- **Согласованные решения:** порог индексации N=3; URL `/tag/<slug>/` (singular) + хаб `/tags`; кейс-мёрж тегов (`Google`+`google` → один `google`); sitemap — вариант B (индексируемые теги попадают в карту сайта сразу на пересборке после перехода порога).
- **`src/lib/tags.ts`** (новый, чистый — без `astro:content`): `tagSlug()` (lowercase + транслит кириллицы + kebab-case; `2гис`→`2gis`, `сквозная-аналитика`→`skvoznaya-analitika`), `TAG_INDEX_THRESHOLD = 3`, `buildTagIndex(entries)` (группировка по slug кейс-нечувствительно, подпись = самое частое написание). Импортируется страницами И `astro.config.mjs`.
- **`src/pages/tag/[slug].astro`** — `getStaticPaths` генерит все теги (ссылки не битые), вёрстка = клон `GeoPage`; ниже порога → `robots="noindex, follow"`, иначе индексируется; `CollectionPage`+`BreadcrumbList`. **`src/pages/tags/index.astro`** — хаб-облако со счётчиками, ссылка добавлена в футер («Разделы»→«Теги»).
- **`BaseLayout.astro`** — новый проп `robots?: string` (дефолт = прежняя строка); meta robots теперь управляемый (пригодится и для пагинации 8.2). **`pages/article/[...slug].astro`** — теги ведут на `/tag/${tagSlug(tag)}/`.
- **`astro.config.mjs`** — sitemap `filter` исключает теги ниже порога: на этапе конфига читаю фронт-маттер с диска (`fs.globSync` + `js-yaml`, добавлен в `dependencies`) и считаю теги тем же `buildTagIndex`. **Автоматика:** тег с 3-м материалом на следующей пересборке сам получает `index,follow` + попадает в sitemap, ручных правок нет.
- Проверено: 44 тега → 98 страниц; 11 индексируемых (в sitemap), ~33 noindex (вне sitemap); `Google`+`google`→`/tag/google/` (4 шт.); склонения верны; build чистый (кроме штатного `WARN reports empty`).

### Сессия 16.07.2026 (скрыть соцсети и подписку до запуска — задача 9)

Закрыта задача 9 из `NEXT_SESSION.md`. Пока нет соцканалов (TG/VK) и рабочей рассылки, их кнопки/формы вели в никуда (`href="#"`, `onsubmit="return false"`). Решение владельца: временно **скрыть, не удаляя** — вернём одним движением.

- **Механизм — единый флаг-модуль `src/lib/flags.ts`:** `SHOW_SOCIALS = false` (ссылки на свои TG/VK), `SHOW_NEWSLETTER = false` (форма подписки + CTA «Подписаться»). Блоки обёрнуты `{FLAG && (…)}`; разметка и CSS-стили (`.socials`/`.newsletter`/`.nl-form`/`.header-cta`/`.mm-cta`) остаются в коде — при `false` не рендерятся. Возврат = `false→true`.
- **Скрыто:** TG/VK в футере (`Footer.astro` `.socials`); форма подписки на главной (`index.astro`) и внизу материала (`article/[...slug].astro`); CTA «Подписаться» в шапке — десктоп (`.header-cta`) и мобильный (`.mm-cta`).
- **Оставлено живым (решение владельца):** (1) **RSS в футере** — канал `/rss.xml` рабочий, `href` исправлен `#`→`/rss.xml`, показывается всегда; (2) **кнопки шаринга TG/VK в статье** — это «поделиться материалом», работают и без своих соцсетей, не трогали.
- Проверено сборкой (чисто, только штатный `WARN reports empty`) + grep по `dist`: в футере только RSS→`/rss.xml`, `header-cta`/`mm-cta`/`newsletter`/`nl-form` в HTML отсутствуют (стили в CSS остались), шаринг в статьях на месте, мёртвых `href="#"` в соцблоке нет.
- **При возврате:** сменить флаг + прописать реальные URL каналов в `Footer.astro` (заодно закроет `sameAs`/`twitter:site` из SEO-задачи 7-B) + подключить провайдера формы (задача 6).

### Сессия 19.07.2026 (База знаний / Spravochnik — Фаза 2 сайта, СМЁРЖЕНО + НА САЙТЕ)

Реализован новый вечнозелёный раздел **`/spravochnik/`** (справочник adtech: компании/технологии/термины/организации) по чеклисту `media-agents/для_кодинга/ТЗ_База_знаний_Фаза2_сайт.md`. Site-часть Фазы 2 (backend Фазы 1 уже в проде в `media-agents`). PR #58 смёржен; **первый реальный материал — Adobe — опубликован (PR #59) и живёт на `/spravochnik/adobe/`.**

- **Коллекция `spravochnik`** (`src/content.config.ts`): `z.discriminatedUnion('type', …)` на 4 типа (`company | technology | term | organization`), у каждого свой набор `facts`. **Поля `author` НЕТ** (решение владельца — материалы без авторства-персоны; в схему не добавлять). `slug` — optional (id/роут Astro берёт из имени файла), схема не `.strict()` — лишние ключи zod отбрасывает, но обязательные `facts` по типу должны быть валидны (напр. `official_url` — валидный URL у company/technology/organization), иначе `astro build` падает.
- **JSON-LD генерится в Astro из `facts`+`type`** (`components/SpravochnikJsonLd.astro`), НЕ пишется LLM во front-matter: `company`/`organization` → `Organization`, `technology` → `SoftwareApplication`, `term` → `DefinedTerm`, + `BreadcrumbList`. `dateModified = updatedDate ?? pubDate`.
- **Роуты:** `pages/spravochnik/[slug].astro` — материал (бейдж типа, карточка `facts` по типу, тело `<Content />`, теги, «Связанные термины» с фильтром существующих slug'ов — без 404); `pages/spravochnik/index.astro` — **алфавитный глоссарий** (латиница A–Z, затем кириллица А–Я, группировка по первой букве), клик по названию → страница термина. **Чистый HTML/CSS, без клиентского JS** (по требованию владельца убран тип-фильтр на чипах — минимализм). Meta/OG/canonical — через общий `BaseLayout` (единая `/og-default.png`, per-slug OG не делаем).
- **Виджет** `components/SpravochnikWidget.astro` в правом рейле главной («Новое в Базе знаний», без иконок; не рендерится, если материалов нет). **Навигация:** пункт «База знаний» в Header (десктоп + бургер) и в колонке «Разделы» футера.
- **SEO:** секция «База знаний» в `/llms.txt` и `/llms-full.txt`; `/spravochnik/*` попадает в sitemap автоматически. Подписи типов — единый `src/lib/spravochnik.ts` (`Компания/Технология/Термин/Организация`).
- **Контракт с пайплайном:** материалы приходят из `media-agents` (`Spravochnik Pipeline`, cron каждые 6 ч) отдельными **draft-PR** (ветка `spravochnik/<slug>`) → владелец смотрит и мёржит вручную (как новости). Авто-мёржа нет.
- ⚠️ **Деплой Timeweb после мержа Adobe с первого раза упал** — но **не из-за кода**: билд-раннер Timeweb трижды не смог склонировать репо (`Failed to connect to github.com port 443 … Could not connect to server`) и упал ДО сборки. Локальный `npm ci && npm run build` на том же `main` — зелёный (131 страница, `/spravochnik/adobe/` собирается). Это тот же документированный сбой инфры Timeweb (см. «Статус запуска» → сноска про `i/o timeout`); лечится кнопкой **▶ (перезапуск деплоя)** — со второго раза прошло.
- ⚠️ **Мелочь контента (не блокер):** пайплайн записал Adobe `facts.subtype: "Публичная компания"` вместо таксономии `platform/agency/broadcaster/media` из ТЗ. Схема принимает любую строку, на странице рендерится «Тип: Публичная компания» — сборку не ломает. Если нужна единая таксономия — правится в промпте писателя `media-agents` (не на сайте).

## Что дальше

Каркас сайта готов целиком (Итерации 1–2); из Итерации 3 закрыты SEO/AI-слой, авторы, служебные страницы, гео-ленты, бургер, страницы тегов (7-H) и подсветка ссылок в теле (8.1). **19.07.2026 добавлен раздел «База знаний» (`/spravochnik/`, Фаза 2) — на сайте, наполняется пайплайном.** Детальная задача — в `NEXT_SESSION.md`. Коротко осталось:

1. **Контент:** демо-материалы вытесняются реальными новостями агентов — чек-лист в `CONTENT_TODO.md`; hero/дайджест/обзоры пока на плейсхолдерах.
2. **Приватный репо + SSH-деплой Timeweb** (сторона владельца; порядок «сначала SSH, потом приватность» — см. `NEXT_SESSION.md`, задача 2).
3. **Подписка** (задача 6) — когда владелец выберет сервис рассылки.
4. **Качество:** Lighthouse ≥90; брендовый email вместо личного, живые TG/VK/RSS в футере. (Счётчик Метрики — подключён 11.07.2026.)
5. Позже — вынести `media-site` в отдельный репозиторий.
