# Задача на следующую сессию — `media-site`, Итерация 3

> Хендофф от сессии 06.07.2026 (вечер). Полный статус — в `CLAUDE.md`, раздел «Статус запуска». Итерация 2 закрыта целиком; список демо-контента на замену — в `CONTENT_TODO.md`.
>
> **Последняя сессия — 14.07.2026.** Закрыты задача 7-H (страницы тегов `/tag/[slug]` + хаб `/tags`, порог индексации ≥3, sitemap-фильтр — PR смёржен) и задача 8.1 (подсветка markdown-ссылок в теле). Детали — в одноимённых блоках `CLAUDE.md`. Ранее (10.07.2026) — задачи 3 (бургер) и 4 (служебные страницы) + гео-ленты, см. блок «Уже закрыто (сессия 10.07.2026)» ниже.

## Контекст (что уже сделано в Итерации 2)

- **Content Collections** (`src/content.config.ts`, Astro 5 glob-loader): `news`, `digest`, `reviews`, `columns`, `reports` (общая zod-схема: title, description ≤160, pubDate, category, geo[], tags[], author-reference, featured, source, readingTime, highlights) + `authors`.
- **Страница материала** `pages/article/[...slug].astro` — единый маршрут `/article/<slug>/` для всех пяти коллекций. Перенос из минимал-макета: крошки → eyebrow → H1 → лид → мета с автором и рабочим шарингом (TG/VK/копировать) → тело → source-box и теги из фронт-маттера → карточка автора → «Читайте также» → подписка. Прогресс-бар чтения работает (проп `reading` у BaseLayout).
- **MDX-компоненты** (`src/components/article/`): `Callout`, `StatBlock`, `CmpTable` (markdown-таблица внутри, стили через `:global`). Передаются в `<Content components={...}>` — импорты в mdx-файлах не нужны.
- **Ленты рубрик**: `/news`, `/digest`, `/reviews`, `/columns`, `/reports` (общий `layouts/SectionPage.astro` + `components/ArticleListItem.astro`). Навигация шапки и «Разделы» футера провязаны.
- **SEO**: `@astrojs/sitemap` (`sitemap-index.xml`), `public/robots.txt` (AI-боты allowed по ТЗ 06), JSON-LD `NewsArticle` + `og:type=article` на материалах.
- **Главная полностью динамическая**: hero = свежий `featured`, «Главное сейчас» = 3 новости (live-точка <3 ч), «Свежее» = 6 (news+reviews), дайджест из `highlights`, «Популярное» = 5 свежих (статистики пока нет), «Мнения» = 2 колонки.
- **Контент**: 13 демонстрационных материалов + профиль автора `authors/y-sokolov.md`. Даты форматируются по Москве (`src/lib/format.ts`), справочник рубрик — `src/lib/sections.ts`.
- Известные мелочи: список `.article-body ul` требовал явного `list-style` (Tailwind preflight) — уже починено; в минифицированном CSS `list-style:disc` выглядит как `list-style:outside` — это норма.

## Уже закрыто из Итерации 3 (сессия 06.07.2026, вечер)

SEO/AI-добивка по ТЗ 06 сделана целиком:
- `/llms.txt` и `/llms-full.txt` — динамические эндпоинты (`src/pages/llms*.txt.ts`), генерируются из коллекций при сборке;
- RSS `/rss.xml` (`@astrojs/rss@^4`, 30 свежих) + `<link rel="alternate">` в head;
- JSON-LD: граф `NewsMediaOrganization`+`WebSite` на всех страницах (BaseLayout), `BreadcrumbList` на материалах и рубриках, `CollectionPage` на рубриках; `NewsArticle` расширен (image, articleSection, keywords, isAccessibleForFree, speakable по `.article-lead`, publisher по `@id`);
- OG: `og:image`/`twitter:image` → `/og-default.png` (1200×630, сгенерирована Playwright-скриншотом, стиль вордмарка), `article:section`/`article:tag`;
- `meta robots` (`max-snippet:-1` и т.д.), самоссылающийся hreflang (`ru` + `x-default`);
- `robots.txt`: + `ChatGPT-User`, `CCBot` Allow; `Diffbot`, `ImagesiftBot` Disallow;
- `logo.png` 512×512 для schema.org.

Осталось из ТЗ 06 (вне кода, делает владелец): Яндекс.Вебмастер + Google Search Console (добавить сайт, отправить sitemap), Метрика/Plausible. В коде: `sameAs` в Organization и `twitter:site` — когда появятся соцканалы; SearchAction — когда будет страница поиска.

## Уже закрыто (сессия 10.07.2026)

- **Замена дефолтной статьи.** Демо-отчёт `reklama-v-geoservisah-2026` (единственный в `reports`, был hero-плейсхолдером) удалён; вместо него — колонка `columns/agentic-commerce-a16z-reklama` (автор `y-sokolov`, категория `adtech-ru`, `featured:true` → занимает hero на главной). Коллекция `reports` теперь пуста: `/reports` показывает штатную заглушку, сборка пишет безобидный `WARN glob-loader … "reports" is empty` (не ошибка, деплой проходит). Вернётся к норме с первым реальным отчётом.
- **Гео-страницы** (было «по желанию» в задаче 5). `/geo` (хаб) + `/geo/russia`, `/geo/world`, `/geo/asia` — новый `layouts/GeoPage.astro` фильтрует все коллекции по `geo` (РФ/МИР/АЗИЯ), с `CollectionPage`+`BreadcrumbList`. В футере «Гео» — заголовок-ссылка на `/geo`, пункты провязаны.
- **Служебные страницы (задача 4 — закрыта).** Новый `layouts/PageLayout.astro` (крошки + H1 + лид + `.article-body`). Созданы `/about`, `/editorial`, `/advertising`, `/contacts`; в футере «Медиа» все четыре ссылки провязаны. На `/contacts` и `/advertising` контактный email — **`yuriysklv@gmail.com` (личный, временно)**; заменить на брендовый, когда появится почта на домене. Страница автора `/authors/y-sokolov` сделана ранее (09.07).
- **Бургер-меню (задача 3 — закрыта).** Панель `.mobile-menu` под шапкой на <900px: рубрики + гео-пилюли + «О нас» + CTA «Подписаться». Иконка бургера → «×»; закрытие по бургеру, тапу вне (бэкдроп), клику по ссылке, Esc и ресайзу до десктопа; фон не прокручивается; `aria-expanded`/`aria-controls`, панель вне tab-порядка в закрытом виде. Ванильный скрипт в `Header.astro`, стили в `global.css`.
- **Правки копий/атрибутов.** Дескриптор в футере → «…в Мире и России для профессионалов рынка.»; роль автора `y-sokolov` → «Руководитель коммерческих продуктов 2ГИС»; на `/about` убрано финальное предложение про трейд-прессу.
- **Новая задача в бэклоге:** подписка на рассылку (задача 6 ниже).

Новые файлы: `layouts/GeoPage.astro`, `layouts/PageLayout.astro`, `pages/geo/{index,russia,world,asia}.astro`, `pages/{about,editorial,advertising,contacts}.astro`, `content/columns/agentic-commerce-a16z-reklama.md`. Изменены: `components/{Header,Footer}.astro`, `styles/global.css`, `content/authors/y-sokolov.md`. Удалён: `content/reports/reklama-v-geoservisah-2026.md`.

## Цель сессии (Итерация 3, адаптированная — деплой, дизайн и SEO уже есть)

### 1. Контент (приоритет)
Заменить демо-материалы реальными по `CONTENT_TODO.md`: проверка фактов, реальные источники, правка фронт-маттера. Рабочий режим: владелец даёт тему/факты — сессия пишет и оформляет. Туда же — статичные тексты (профиль автора, «О нас»).

### 2. Приватный репозиторий + деплой Timeweb по SSH
Сделать репо `yuriysklv-hue/media` закрытым, не сломав автодеплой. **Порядок важен — сначала SSH, потом приватность:**
1. В панели Timeweb (приложение → Настройки → подключение репозитория) переключить способ доступа на **SSH**: Timeweb выдаёт/просит публичный SSH-ключ.
2. Добавить этот ключ в GitHub: репо → Settings → **Deploy keys** → Add deploy key (доступ read-only достаточен), URL репо в Timeweb — `git@github.com:yuriysklv-hue/media.git`.
3. Прогнать тестовый деплой (кнопка ▶) — убедиться, что клонирование по SSH работает **до** смены видимости.
4. GitHub → Settings → General → Danger Zone → **Change visibility → Private**.
5. Снова прогнать деплой и проверить автодеплой пушем в `main`.
Заметки: рабочие сессии Claude ходят в репо через GitHub App — приватность им не мешает; вебхук автодеплоя тоже сохраняется. Если у Timeweb «SSH-способ» выглядит иначе (например, только через их GitHub-интеграцию) — задокументировать фактический путь в CLAUDE.md.

### 3. Бургер-меню (мобильная навигация) — ✅ СДЕЛАНО (10.07.2026)
> Реализовано, см. блок «Уже закрыто (сессия 10.07.2026)». Исходное ТЗ ниже — для истории.

Кнопка-бургер в шапке сейчас декоративная (на <900px скрывается `.nav` и CTA, бургер ничего не открывает). Сделать открывающуюся панель. **Состав согласовать с владельцем на старте сессии**, предложение по умолчанию:
- пять рубрик (Новости / Дайджесты / Обзоры / Отчёты / Мнения) — как в десктоп-навигации;
- CTA «Подписаться»;
- опционально вторым блоком: гео (Россия / Мир / Азия — когда появятся гео-ленты) и «О нас».
Реализация: оверлей/выпадающая панель под шапкой в стиле макета (белый фон, линейки, sans), доступность (aria-expanded, закрытие по Esc и тапу вне), без JS-фреймворков — ванильный скрипт как в BaseLayout. Тот же пункт закрывает вопрос «куда ведут ссылки шапки на мобиле».

### 4. Служебные страницы — ✅ СДЕЛАНО (10.07.2026 + 09.07.2026)
- ✅ `/about` («О нас») — сделано;
- ✅ страница автора `/authors/y-sokolov` + schema (сделано 09.07);
- ✅ пункты футера «Реклама на сайте» (`/advertising`), «Редакция» (`/editorial`), «Контакты» (`/contacts`) — сделаны.

Остаточный хвост: заменить личный email на брендовый на `/contacts` и `/advertising`; оживить иконки TG/VK/RSS в футере, когда появятся соцканалы.

### 5. Качество
- Lighthouse (astro preview + playwright/lighthouse) — цель ≥90 Performance;
- ✅ Яндекс.Метрика — счётчик `110606034` подключён (11.07.2026);
- проверить фронт на реальном объёме контента (когда появятся длинные статьи);
- ✅ гео-ленты `/geo/russia|world|asia` + хаб `/geo` — сделано (10.07.2026), футер «Гео» провязан.

### 6. Подписка на рассылку (форма + размещение)
Сейчас блок подписки — **нерабочая заглушка** и стоит **непоследовательно**. Нужно довести до ума.

Текущее состояние:
- Форма (`.newsletter` + `.nl-form`) есть только на **главной** (`pages/index.astro`) и внизу **страницы материала** (`pages/article/[...slug].astro`). Обе — заглушки: `onsubmit="return false"`, email никуда не уходит.
- CTA «Подписаться» в шапке (`components/Header.astro`, десктоп + мобильное меню `.mm-cta`) — **без действия** вовсе.
- Формы **нет** на страницах рубрик (`SectionPage`), гео-лентах (`GeoPage`) и статических страницах (`PageLayout`) — а именно туда приходят из поиска/соцсетей.

Что сделать:
1. **Заставить форму работать.** Выбрать способ сбора email без своего бэкенда (сайт статический на Timeweb): внешний сервис форм/рассылки (напр. отправка на почту через сторонний endpoint, или провайдер рассылок с JS-виджетом/API). Согласовать с владельцем сервис и куда падают адреса. Учесть валидацию, состояние «спасибо/ошибка», защиту от спама, согласие на обработку ПДн (152-ФЗ) — чекбокс + ссылка на политику.
2. **CTA «Подписаться» в шапке** — привязать к форме: скролл к блоку на текущей странице либо к `/#subscribe` (добавить якорь у блока на главной), либо открывать модалку с формой. Один механизм на десктоп и бургер.
3. **Единый компонент и релевантное размещение.** Вынести блок в переиспользуемый компонент (`components/Newsletter.astro`) и показывать консистентно: точно на главной и материалах; решить по рубрикам/гео/статике — вероятно, компактный вариант в подвале контента, но не на служебных (Контакты/Реклама/О нас — там перегруз). Согласовать матрицу «где показываем».

Оговорка: провайдер и endpoint должен дать владелец (аккаунт сервиса, ключ/URL). До этого — задача в статусе «ждём решения по сервису».

### 7. SEO-задачи

> Консолидированный список открытых SEO-хвостов. Раньше они были размазаны по строке «Осталось из ТЗ 06» (выше) и разделу «5. Качество» — здесь единый источник. Полное ТЗ — `06_SEO_GEO_и_AI-разметка.md` (чек-листы: раздел 11 «на каждую публикацию», раздел 12 «при запуске сайта»).
>
> **Кодовый слой ТЗ 06 закрыт** (sitemap, robots.txt, `llms.txt`/`llms-full.txt`, RSS, JSON-LD-граф `NewsMediaOrganization`+`WebSite`+`NewsArticle`+`BreadcrumbList`+`CollectionPage`, OG-картинка, hreflang, meta robots) — см. блок «Уже закрыто из Итерации 3». Ниже — только то, что осталось.

**A. Сторона владельца (вне кода, нужны внешние аккаунты/данные):**
- [x] **Яндекс.Вебмастер** — сайт добавлен, права подтверждены мета-тегом (`yandex-verification` в `BaseLayout`, задеплоено 12.07.2026), `sitemap-index.xml` отправлен (12.07.2026). Осталось: следить за индексацией/ИКС.
- [x] **Google Search Console** — сайт добавлен, права подтверждены мета-тегом (`google-site-verification` в `BaseLayout`, задеплоено 11.07.2026), `sitemap-index.xml` отправлен (12.07.2026). Осталось: следить за индексацией.
- [x] **Bing Webmaster Tools** — сайт добавлен, права подтверждены мета-тегом (`msvalidate.01` в `BaseLayout`, задеплоено 11.07.2026), `sitemap-index.xml` отправлен (12.07.2026). Важно для LLM-видимости: на индекс Bing опираются веб-поиск ChatGPT и Perplexity (см. врезку «Как SEO связано с LLM-выдачей» ниже).
- [x] **Яндекс.Метрика** — счётчик `110606034` подключён в `BaseLayout` (`is:inline`, опции webvisor/clickmap/trackLinks/accurateTrackBounce), данные принимаются (11.07.2026). Помимо аналитики даёт Яндексу «обход по счётчику Метрики» (ускоренная индексация) + поведенческие факторы. Plausible/Umami/GA не ставим.
- [x] **Валидация разметки после деплоя** (12.07.2026): PageSpeed Insights (моб.) — Performance 99, a11y 93, Best Practices 96, SEO 100; schema.org Validator по главной — «нет ошибок, нет предупреждений» (граф `WebSite`+`NewsMediaOrganization`); по URL материала — `NewsArticle`, `BreadcrumbList`, `WebSite` все три без ошибок и предупреждений; Google Rich Results Test по главной — «Организация, 1 элемент без ошибок». Замечания раздела a11y (контраст + порядок заголовков, из-за них 93) заведены отдельной задачей — см. **G** ниже.

**B. В коде (ждут внешних данных — сделать, когда данные появятся):**
- [ ] `sameAs` в `NewsMediaOrganization` + `twitter:site` — когда появятся соцканалы (TG/VK).
- [ ] 301-редиректы типовых опечаток (`/news/` → `/news`) и HSTS-заголовок (уровень Timeweb/Astro).

**C. Производительность (SEO-фактор, раздел 9 ТЗ 06):**
- [ ] Lighthouse / PageSpeed ≥90 Performance (astro preview + playwright). Дублируется с задачей «5. Качество» — ведём здесь.
- [ ] Проверить Core Web Vitals на реальном объёме контента (LCP <2.5s, INP <200ms, CLS <0.1).

**D. Расширения разметки из ТЗ 06, ещё не заведённые (кандидаты, по мере надобности):**
- [ ] `contentLocation` (`Place`/`City`) для материалов с гео-привязкой — раздел 6.3.
- [ ] `FAQPage` для материалов с Q&A-блоками — раздел 3.6.
- [ ] `Article` + `Dataset` для отчётов — раздел 3.3 (актуально с первым реальным отчётом; коллекция `reports` сейчас пуста).
- [ ] TDMRep (`/.well-known/tdmrep.json`) — раздел 7.5, опционально (когда появится платный/ограниченный контент).
- [ ] Метрики AI-цитируемости (рефереры от chatgpt.com / perplexity.ai / claude.ai) — раздел 10, план «Релиз 3».

**E. Разметка цитат (решение 12.07.2026: спроектировать, потом решить — приоритет СРЕДНИЙ).**
- [ ] **Цитаты не размечены schema.org, хотя они в каждой второй новости.** Агентский пайплайн пишет прямые цитаты спикеров/источников, но в вёрстке нет `Quotation`/`citation` (или аналога) — LLM и поисковики не распознают их как цитаты первоисточника.
  - **Конкретный пример (владелец, 12.07.2026):** `https://1screen.ru/article/iab-predlagayet-yedinyy-yazyk-dlya-ryinka-tsifrovogo-video/` — цитаты Финштейн написаны **инлайном внутри абзацев** («…», — пояснила Финштейн), а не выносными блоками. Проверено в контенте: это обычный `<p>`, без markdown-blockquote (`>`), поэтому ни `<blockquote>`, ни `<q>`, ни schema не появляются. `blockquote` в стилях есть только для карточек мнений (`.op-card blockquote`), к телу статьи не относится.
  - **Развилка при реализации (решаем тогда, не сейчас):**
    1. **Инлайновые цитаты** — семантически это `<q cite="…">` + микроразметка, а не выносной blockquote (вынос сломает поток чтения). schema.org `Quotation` с `spokenByCharacter`/`citation` — кандидат, но поддержка в rich-результатах слабая.
    2. **Где генерить разметку** — в шаблоне (`media-site`, `pages/article/[...slug].astro` + MDX) по приходящему тексту, либо структурно в пайплайне (`media-agents`: отдельное поле цитаты/спикера во фронт-маттере или разметка в теле). Правка в пайплайне — правильнее для потока, но затрагивает два репо.
    3. Отдельно решить про **выносные pull-quotes** (дизайн-элемент) vs инлайновые — это разные сущности.
  - Задача — сперва спроектировать (что размечаем, где, каким тегом), затем реализовать. Не начинать правку вслепую.

**F. Рост SEO и LLM-видимости — вынесено в отдельный ресёрч.**
> Быстрые задачи (A–E) закрывают базу. Стратегический трек роста разбираем отдельно — не детализируем здесь. Направления, которые войдут в ресёрч (чтобы не потерять):
> - **IndexNow** — мгновенная индексация Bing + Яндекс (техзадача в код). **Отложено (решение 12.07.2026):** ждём роста потока статей — подключения источников Россия и Азия в пайплайне `media-agents`; пока публикаций мало, выгода от мгновенного пинга невелика. При реализации продумать, откуда слать пинг — при мерже статьи в `media-agents`, а не только при сборке сайта.
> - **Оригинальные тексты** в Яндекс.Вебмастере — защита авторства перед публикацией;
> - **Флагманский отчёт с оригинальными данными** — наполнить пустую коллекцию `reports` (линкбейт + цитируемость LLM; пересекается с `CONTENT_TODO.md`);
> - **Линкбилдинг**: список RU-доноров по теме (Sostav, AdIndex, Cossa, ppc.world, Roem, VC, Habr; ассоциации АРИР/АКАР/РАМУ) + **Pressfeed** (журзапросы);
> - **Глоссарий adtech** (`DefinedTerm`) — определительные запросы;
> - RU-сообщества вместо Reddit (Habr / VC / Яндекс Кью / Telegram); свой TG-канал как SEO-инструмент (брендовые запросы).
> Reddit и навык Алисы — признаны нерелевантными (см. обсуждение).

**G. Доступность (a11y) — замечания PageSpeed — ✅ СДЕЛАНО (12.07.2026).**
> Источник: PageSpeed Insights (моб.), раздел «Специальные возможности» — 93 балла. Оба замечания исправлены в вёрстке/CSS `media-site`, дизайн сохранён (проверено скриншотами + программной проверкой heading-order на 16 страницах: у каждой ровно один h1, пропусков уровней нет). **Осталось у владельца: перепрогнать PageSpeed после деплоя — ждём a11y ≥ 95–100.** Исходное ТЗ ниже — для истории.

- [x] **G1. Недостаточный контраст приглушённого текста.** Все замечания контраста завязаны на один токен — `--ink-3` (`src/styles/global.css`): сейчас `#868f9e`, на белом даёт **≈3.3:1**, на поднятых поверхностях `#f5f7f8` — **≈3.0:1**, а WCAG AA для мелкого текста требует **4.5:1**. Затрагивает: логотип `.tld` («.ru»), гео-теги `.geo` (РФ/Мир/Азия), мету `.mono`/`.meta-row` (даты, время чтения), относительное время `.side-item .m` / `.rel-card .m`, роль автора `.r`, `.nl-note`, хлебные крошки `.breadcrumbs`, `::marker` списков, подписи футера и т.п. — все через `var(--ink-3)`.
  - **Фикс:** затемнить токен `--ink-3` в **светлой** теме до значения с контрастом ≥4.5:1 и на `#fff`, и на `#f5f7f8`. Проверенные кандидаты (посчитаны по WCAG): `#636d7d` (white **5.23**, subtle **4.87**) — рекомендуемый; `#616a79` (5.46 / 5.08) — с большим запасом. Не брать `#6a7280` (4.85 / **4.51** — впритык на subtle).
  - **Тёмная тема:** там `--ink-3: #6e7789` на `#0b0d11` даёт **≈4.3:1** (тоже ниже нормы) — заодно осветлить до `#828b9c` (darkbg **5.67**, surface **5.19**) или `#8a93a4` (6.29 / 5.76). PageSpeed тестировал светлую, но чиним обе для реальной доступности.
  - `--ink-3` меняется в трёх местах файла: строка ~19 (`:root`), ~48 (`prefers-color-scheme` / `[data-theme=light]`), ~41 и ~54 (dark). Свести к консистентным значениям светлая/тёмная.
  - ⚠️ Не трогать `--ink-2` (`#545d6c`, ≈6.6:1 — проходит) и `--accent-strong` (для ссылок — проходит). Проблема только в `--ink-3`.
- [x] **G2. Пропуски уровней заголовков (heading order).** На главной (`src/pages/index.astro`) секционные подписи свёрстаны как `<div>`, из-за чего уровни идут не по порядку:
  - `<h1>` (hero) → сразу `<h3>` в карточках «Главное сейчас» (подпись `.side-head` — `<div>`, строка ~68) — **пропуск h2** (это и есть флаг PageSpeed на `<h3>` «Meta запатентовала…»);
  - сайд-бар: `.rail-title` «Дайджест недели» / «Популярное за неделю» — `<div>`, а элементы «Популярного» — `<h4>` (`.pop-item`, строка ~109) — **пропуск**;
  - футер (`src/components/Footer.astro`, строка ~53): колонки «РАЗДЕЛЫ»/«МЕДИА» — `<h4>` после `<h2>` страницы — **пропуск h3**.
  - **Фикс (согласовать конкретику при реализации):** привести к строгой иерархии h1→h2→h3 без пропусков, **сохранив внешний вид**. Варианты: (а) секционные подписи-`<div>` (`.side-head`, `.rail-title`) сделать реальными заголовками нужного уровня (напр. `<h2 class="side-head">`), а карточки под ними опустить на уровень ниже (`<h3>`/`<h4>` → на шаг глубже родителя); (б) элементы «Популярного» перестать держать заголовками, если они не нужны как заголовки. Футерные `<h4>` → `<h2>` (футер — отдельный лендмарк, h2 допустим) либо `<h3>`. При замене `<div>`→`<hN>` сохранить класс на элементе и сбросить дефолтные отступы/размер заголовка через CSS (Tailwind preflight обнуляет, но проверить), чтобы вёрстка не «прыгнула».
  - Проверить остальные шаблоны на ту же болезнь: `layouts/SectionPage.astro`, `GeoPage.astro`, `PageLayout.astro`, страница материала `pages/article/[...slug].astro`, `pages/authors/[slug].astro` — там тоже могут быть `.rail-title`/подписи-`<div>` и футер.

**H. Страницы тегов (ревью владельца 12.07.2026 — приоритет СРЕДНИЙ-ВЫСОКИЙ) — ✅ СДЕЛАНО (14.07.2026).**
- [x] **Теги под материалами кликабельны, но ведут в никуда.** Было: в `pages/article/[...slug].astro` теги рендерились как `<a href="#">` — мёртвые ссылки, страниц тегов не было. Сделано целиком: страницы тегов + хаб + провязка ссылок.
  - **Согласованные решения (владелец, 14.07.2026):** порог N=3; схема URL `/tag/<slug>/` (singular); хаб `/tags` — делаем; кейс-мёрж тегов вкл. (`Google`+`google` → один `google`); sitemap — **вариант B** (индексируемые теги попадают в карту сайта сразу на пересборке после перехода порога).
  - **Реализация:**
    - `src/lib/tags.ts` — чистый модуль (без `astro:content`): `tagSlug()` (lowercase + транслит кириллицы + kebab-case), `TAG_INDEX_THRESHOLD = 3`, `buildTagIndex(entries)` (группировка по slug кейс-нечувствительно, подпись = самое частое исходное написание, дубли внутри записи не удваивают счётчик). Импортируется и страницами, и `astro.config.mjs`.
    - `src/pages/tag/[slug].astro` — `getStaticPaths` генерит **все** теги (ссылки не битые), вёрстка = клон `GeoPage` (крошки Главная → Теги → #тег, `<h1>#тег</h1>`, лента `ArticleListItem headingLevel={2}`, склонение «материал/материала/материалов»), `CollectionPage`+`BreadcrumbList` JSON-LD, самоссылающийся canonical (из BaseLayout). `robots` = `noindex, follow` при `count < 3`, иначе дефолт.
    - `src/pages/tags/index.astro` — хаб-облако всех тегов со счётчиками (через `PageLayout`), индексируется. Ссылка на хаб добавлена в футер («Разделы» → «Теги»).
    - `BaseLayout.astro` — новый проп `robots?: string` (дефолт = прежняя строка `index, follow, …`); хардкод meta заменён на `content={robots}`. Переиспользуемо (пригодится для пагинации 8.2).
    - `astro.config.mjs` — sitemap `filter`: на этапе конфига читаю фронт-маттер с диска (`fs.globSync` + `js-yaml`, добавлен в `dependencies`), считаю теги тем же `buildTagIndex` и исключаю из sitemap теги ниже порога. Индексируемые + хаб остаются.
  - **Проверено (build + скриншоты + grep dist):** 44 уникальных тега, 98 страниц; индексируемых тегов 11 (programmatic 21 … openai 3) — `index,follow` + в sitemap; ~33 тонких — `noindex,follow` + вне sitemap; хаб `/tags` в sitemap; `Google`+`google` слиты в `/tag/google/` (4 материала); склонения (21→материал, 8→материалов, 4→материала, 1→материал) верны; ссылки в статьях ведут на `/tag/<slug>/`.
  - **Автоматика:** когда тег набирает 3-й материал, на следующей пересборке (мёрж PR → автодеплой Timeweb) он сам получает `index,follow` и попадает в sitemap. Ручных действий нет; порог — одна константа `TAG_INDEX_THRESHOLD`.

<!-- Место для новых SEO-задач от владельца — впишем сюда после обсуждения. -->

---

### Врезка: как SEO-задачи связаны с выдачей в LLM-чатах

Чтобы попадать в ответы ChatGPT, Perplexity, Gemini, YandexGPT и т.п., работает **не отдельная «магия для ИИ», а обычная индексация + структура под цитирование.** Два канала:

1. **Обучающий корпус** (боты `GPTBot`/`ClaudeBot`/`CCBot` из `robots.txt`) — модель обучается на скрейпе с лагом в месяцы; для нового сайта в моменте почти не работает. У нас пущены «на вырост».
2. **Живой веб-поиск в момент ответа (RAG)** — главный канал для нового сайта: чат ищет по вебу, читает верхние страницы, цитирует со ссылкой. Почти все грундятся на существующих поисковых индексах:
   - ChatGPT Search, Perplexity, Copilot → **Bing** (+ свои краулеры);
   - Gemini / AI Overviews → **Google**;
   - YandexGPT / Нейро → **Яндекс**.

**Практический вывод:** индексация в Google + **Bing** + Яндексе = фундамент LLM-видимости (отсюда обязательный Bing Webmaster Tools в задаче A). `llms.txt`/`llms-full.txt` — молодой стандарт, поддержка у движков слабая, держим «на вырост». Чистая структура (H1→H2, первый абзац = саммари, факты с источниками, размеченные цитаты) → LLM легче вытащить готовую цитату именно из нас.

---

## 8. Вёрстка и вывод контента (ревью владельца 12.07.2026)

> Разобрано в сессии 12.07.2026 по списку владельца. Часть пунктов оказались вопросами «как устроено» (ответы зафиксированы ниже), часть — задачами. Приоритеты проставлены.

### 8.1. Подсветка ссылок в теле статьи — ✅ СДЕЛАНО (14.07.2026)
- [x] **Markdown-ссылки в теле никак не выделены.** Пример владельца: `https://1screen.ru/article/mrc-opublikoval-vremennye-principy-dlya-ii-v-mediamereniyah/` — «доступен [по ссылке]» активна, но визуально не отличается от текста. Причина найдена: в `global.css` стилизован только `.article-body a.inline` (подчёркивание + акцент), а markdown-ссылки `[текст](url)` рендерятся как `<a>` **без** класса `.inline` → без стилей.
  - **Сделано:** базовый стиль расширен на все ссылки тела — селектор `.article-body a, .article-body a.inline` (акцент `--accent-strong` + подчёркивание `text-underline-offset:2px`, hover → `--accent`); `.inline` оставлен для обратной совместимости (теперь дублирует базу). Спец-блоки внутри `.article-body` проверены: `.source-box a` уже задаёт тот же акцент+underline (равная специфичность, идёт ниже — вид не изменился); тег-пилюли `.tag-list a` НЕ задавали `text-decoration`, поэтому им явно добавлен `text-decoration:none`, иначе база их подчёркивала. Проверено сборкой (чисто, кроме штатного `WARN reports empty`) + скриншотом и `getComputedStyle` на реальной статье: прозаические ссылки — `rgb(10,90,98)`+underline, пилюли — без подчёркивания, source-box без изменений.

### 8.2. Пагинация рубрик/гео — приоритет НИЗКИЙ (отложено до порога, решение 12.07.2026)
- [ ] **Гипотеза владельца «не все материалы на сайте из-за отсутствия пагинации» — НЕ подтвердилась.** Проверено: `SectionPage.astro` и `GeoPage.astro` выводят **все** материалы коллекции без ограничения (`.sort()` без `.slice()`), у каждого материала своя страница `/article/<slug>/`. Ограничение 3/6/5 есть только на **главной** — это витрина, by design. Весь контент доступен.
  - **Реальная задача — масштабируемость:** когда в рубрике накопится много материалов (порог ~30–50), единый список станет тяжёлым (вес страницы, UX, LCP). Тогда добавить пагинацию.
  - **Отложено** до достижения порога (решение владельца 12.07.2026). При реализации — `getStaticPaths` + `paginate()` Astro на `SectionPage`/`GeoPage`; страницы 2+ → `rel=prev/next` и `noindex,follow` (ТЗ 06 разд. 8), canonical на себя. Триггер — рост потока статей (источники Россия/Азия в `media-agents`).

### 8.3. Дайджест недели — как устроено (справка, действий не требуется)
- **Ответ на вопрос владельца.** Дайджест — это **полноценная статья** в коллекции `digest` (своя страница `/article/<week>/`, напр. `/article/2026-w27/`, поля `highlights[]` и `week` во фронт-маттере). Блок «Дайджест недели» в правом сайдбаре главной — **тизер**: ссылка на статью-дайджест (`.d-head`) + буллеты из `highlights`.
- **Синхронизация с пайплайном — через контент-коллекцию, автоматическая.** Агент `media-agents` кладёт `.md` в `src/content/digest/`; при мерже PR главная сама берёт самый свежий (`all.find(e => e.collection==='digest')`). Сегодняшний дайджест после мержа появится: (1) тизер на главной, (2) лента `/digest`, (3) своя статья. На главной показывается только один (свежий) дайджест — норма.
- [ ] **Мелкая проверка (после первого боевого дайджеста):** убедиться, что тизер и буллеты `highlights` корректно отрисовались, ссылка ведёт на статью, дата/`week` верные. Разовая проверка, не код-задача.

### 8.4. Логика «Главное сейчас» vs «Свежее» — как устроено (справка, действий не требуется)
- **Ответ на вопрос владельца** (`index.astro`): **Hero** — свежий `featured` (иначе свежий не-новостной). **«Главное сейчас»** = 3 самые свежие **новости** кроме hero (live-точка, если материал младше 3 ч). **«Свежее»** = следующие материалы (**news + reviews**) кроме hero и тех трёх, до 6 штук. Пересечений нет: «Главное» — верхушка новостной ленты, «Свежее» — продолжение. Работает by design; менять только если владелец захочет иную витрину (тогда завести отдельную задачу).

## 9. Скрыть соцсети (TG/VK) и подписку до запуска — приоритет СРЕДНИЙ (заведено 16.07.2026)

> Пока у медиа нет ни соцканалов (Telegram/VK), ни работающей рассылки, кнопки и формы ведут в никуда (`href="#"`, `onsubmit="return false"`) — пустые заглушки. Решение владельца: **временно скрыть, не удалять.** Разметку и стили оставляем в коде (закомментировать / флаг / `hidden`), чтобы вернуть одним движением, когда появятся каналы и сервис рассылки. Пересекается с задачей 6 (подписка) и хвостом задачи 4 («оживить иконки TG/VK/RSS»).

**Что скрыть (ничего не удалять безвозвратно):**
- [ ] **Соц-ссылки TG/VK в футере** (`components/Footer.astro`, блок `.socials`, строки ~46–50): `TG` и `VK` ведут на `#`. Скрыть их. По `RSS` — решить отдельно: канал `/rss.xml` рабочий, ссылку можно оставить живой (поправить `href` на `/rss.xml`) либо скрыть вместе с остальными — согласовать с владельцем.
- [ ] **Кнопки шаринга TG/VK в статье** (`pages/article/[...slug].astro`, мета-строка с шарингом TG/VK/«Ссылка»). ⚠️ Это функциональные кнопки «поделиться материалом» (шарят статью в TG/VK), а не ссылки на наши каналы — они работают и без своих соцсетей. Уточнить у владельца: скрывать ли шаринг тоже, или он имел в виду только ссылки-на-наши-каналы (футер). По умолчанию — не трогать шаринг, скрыть только «мёртвые» ссылки на несуществующие каналы.
- [ ] **Форма подписки** (`.newsletter` + `.nl-form`) — на главной (`pages/index.astro`) и внизу страницы материала (`pages/article/[...slug].astro`). Скрыть блок целиком (заглушка `onsubmit="return false"`, email никуда не уходит).
- [ ] **CTA «Подписаться» в шапке — десктоп** (`components/Header.astro`, кнопка в `.nav`/десктоп-области) — скрыть. Мобильный CTA `.mm-cta` в бургер-меню — согласовать: скрывать ли заодно (по логике задачи — да, форма всё равно скрыта).

**Как скрывать (чтобы легко вернуть):**
- Предпочтительно единый флаг видимости (напр. константа `SHOW_SOCIALS` / `SHOW_NEWSLETTER` в общем месте или в конфиге) — тогда возврат = смена `false→true`. Либо аккуратное комментирование блоков с пометкой `<!-- вернуть, когда появятся соцсети/рассылка -->`. Не вырезать CSS-стили `.socials`/`.newsletter`/`.nl-form`/CTA — оставить в `global.css`.
- Проверить, что скрытие не ломает раскладку (пустой `.footer-brand` без соцсетей, отступы под скрытой формой), сборку и a11y (нет ссылок на `#`).

**Возврат (когда появятся каналы и сервис рассылки):** снять флаг/раскомментировать, прописать реальные URL каналов в футере (закрывает и `sameAs`/`twitter:site` из задачи 7-B), подключить рабочий провайдер формы (задача 6).

## Подводные камни

- Канон дизайна — `mockups/1screen-mockup-minimal.html` и CLAUDE.md (НЕ ТЗ 02/09: там serif и старая палитра).
- Всё в `media-site/`; сборка `cd media-site && npm run build`. Рабочая ветка сессии ≠ `main`: деплой случится только после мержа PR в `main` (делает владелец).
- Playwright — глобально: `import pkg from '/opt/node22/lib/node_modules/playwright/index.js'; const { chromium } = pkg;`.
- `@astrojs/mdx` держать на `^4` (7.x требует Astro 7).
- Даты контента: `pubDate` указывать с `+03:00`; хелперы формата уже считают всё по Europe/Moscow.
- **Деплой Timeweb иногда падает по их вине** (`i/o timeout`, `Could not resolve host: github.com` в логе) — код ни при чём, лечится кнопкой ▶ (перезапуск деплоя) через несколько минут. Проверено 06.07.2026.
- Приватность репо менять только **после** проверенного SSH-доступа Timeweb (см. задачу 2), иначе автодеплой сломается.

## Критерий приёмки

- Build чистый (кроме безобидного `WARN … "reports" is empty` — см. выше);
- репо приватный, автодеплой Timeweb работает по SSH (проверено пушем в `main`) — **осталось**;
- ✅ бургер открывает мобильное меню на <900px;
- реальные материалы проходят схему коллекций и корректно выглядят на главной, в рубриках и на странице материала.
