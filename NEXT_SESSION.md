# Задача на следующую сессию — `media-site`, Итерация 3

> Хендофф от сессии 06.07.2026 (вечер). Полный статус — в `CLAUDE.md`, раздел «Статус запуска». Итерация 2 закрыта целиком; список демо-контента на замену — в `CONTENT_TODO.md`.
>
> **Последняя сессия — 10.07.2026.** Закрыты задачи 3 (бургер) и 4 (служебные страницы) + гео-ленты из задачи 5; см. блок «Уже закрыто (сессия 10.07.2026)» ниже.

## Контекст (что уже сделано в Итерации 2)

- **Content Collections** (`src/content.config.ts`, Astro 5 glob-loader): `news`, `digest`, `reviews`, `columns`, `reports` (общая zod-схема: title, description ≤160, pubDate, category, geo[], tags[], author-reference, featured, source, readingTime, highlights) + `authors`.
- **Страница материала** `pages/article/[...slug].astro` — единый маршрут `/article/<slug>/` для всех пяти коллекций. Перенос из минимал-макета: крошки → eyebrow → H1 → лид → мета с автором и рабочим шарингом (TG/VK/копировать) → тело → source-box и теги из фронт-маттера → карточка автора → «Читайте также» → подписка. Прогресс-бар чтения работает (проп `reading` у BaseLayout).
- **MDX-компоненты** (`src/components/article/`): `Callout`, `StatBlock`, `CmpTable` (markdown-таблица внутри, стили через `:global`). Передаются в `<Content components={...}>` — импорты в mdx-файлах не нужны.
- **Ленты рубрик**: `/news`, `/digest`, `/reviews`, `/columns`, `/reports` (общий `layouts/SectionPage.astro` + `components/ArticleListItem.astro`). Навигация шапки и «Разделы» футера провязаны.
- **SEO**: `@astrojs/sitemap` (`sitemap-index.xml`), `public/robots.txt` (AI-боты allowed по ТЗ 06), JSON-LD `NewsArticle` + `og:type=article` на материалах.
- **Главная полностью динамическая**: hero = свежий `featured`, «Главное сейчас» = 3 новости (live-точка <3 ч), «Свежее» = 6 (news+reviews), дайджест из `highlights`, «Популярное» = 5 свежих (статистики пока нет), «Мнения» = 2 колонки.
- **Контент**: 13 демонстрационных материалов + профиль автора `authors/y-sokolov.md`. Даты форматируются по Москве (`src/lib/format.ts`), справочник рубрик — `src/lib/sections.ts`.
- Известные мелочи: список `.article-body ul` требовал явного `list-style` (Tailwind preflight) — уже починено; в минифицированном CSS `list-style:disc` выглядит как `list-style:outside` — это норма.

## Уже закрыто из Итерации 3 (сессия 06.07.2026, вечер)

SEO/AI-добивка по ТЗ 06 сделана целиком:
- `/llms.txt` и `/llms-full.txt` — динамические эндпоинты (`src/pages/llms*.txt.ts`), генерируются из коллекций при сборке;
- RSS `/rss.xml` (`@astrojs/rss@^4`, 30 свежих) + `<link rel="alternate">` в head;
- JSON-LD: граф `NewsMediaOrganization`+`WebSite` на всех страницах (BaseLayout), `BreadcrumbList` на материалах и рубриках, `CollectionPage` на рубриках; `NewsArticle` расширен (image, articleSection, keywords, isAccessibleForFree, speakable по `.article-lead`, publisher по `@id`);
- OG: `og:image`/`twitter:image` → `/og-default.png` (1200×630, сгенерирована Playwright-скриншотом, стиль вордмарка), `article:section`/`article:tag`;
- `meta robots` (`max-snippet:-1` и т.д.), самоссылающийся hreflang (`ru` + `x-default`);
- `robots.txt`: + `ChatGPT-User`, `CCBot` Allow; `Diffbot`, `ImagesiftBot` Disallow;
- `logo.png` 512×512 для schema.org.

Осталось из ТЗ 06 (вне кода, делает владелец): Яндекс.Вебмастер + Google Search Console (добавить сайт, отправить sitemap), Метрика/Plausible. В коде: `sameAs` в Organization и `twitter:site` — когда появятся соцканалы; SearchAction — когда будет страница поиска.

## Уже закрыто (сессия 10.07.2026)

- **Замена дефолтной статьи.** Демо-отчёт `reklama-v-geoservisah-2026` (единственный в `reports`, был hero-плейсхолдером) удалён; вместо него — колонка `columns/agentic-commerce-a16z-reklama` (автор `y-sokolov`, категория `adtech-ru`, `featured:true` → занимает hero на главной). Коллекция `reports` теперь пуста: `/reports` показывает штатную заглушку, сборка пишет безобидный `WARN glob-loader … "reports" is empty` (не ошибка, деплой проходит). Вернётся к норме с первым реальным отчётом.
- **Гео-страницы** (было «по желанию» в задаче 5). `/geo` (хаб) + `/geo/russia`, `/geo/world`, `/geo/asia` — новый `layouts/GeoPage.astro` фильтрует все коллекции по `geo` (РФ/МИР/АЗИЯ), с `CollectionPage`+`BreadcrumbList`. В футере «Гео» — заголовок-ссылка на `/geo`, пункты провязаны.
- **Служебные страницы (задача 4 — закрыта).** Новый `layouts/PageLayout.astro` (крошки + H1 + лид + `.article-body`). Созданы `/about`, `/editorial`, `/advertising`, `/contacts`; в футере «Медиа» все четыре ссылки провязаны. На `/contacts` и `/advertising` контактный email — **`yuriysklv@gmail.com` (личный, временно)**; заменить на брендовый, когда появится почта на домене. Страница автора `/authors/y-sokolov` сделана ранее (09.07).
- **Бургер-меню (задача 3 — закрыта).** Панель `.mobile-menu` под шапкой на <900px: рубрики + гео-пилюли + «О нас» + CTA «Подписаться». Иконка бургера → «×»; закрытие по бургеру, тапу вне (бэкдроп), клику по ссылке, Esc и ресайзу до десктопа; фон не прокручивается; `aria-expanded`/`aria-controls`, панель вне tab-порядка в закрытом виде. Ванильный скрипт в `Header.astro`, стили в `global.css`.
- **Правки копий/атрибутов.** Дескриптор в футере → «…в Мире и России для профессионалов рынка.»; роль автора `y-sokolov` → «Руководитель коммерческих продуктов 2ГИС»; на `/about` убрано финальное предложение про трейд-прессу.
- **Новая задача в бэклоге:** подписка на рассылку (задача 6 ниже).

Новые файлы: `layouts/GeoPage.astro`, `layouts/PageLayout.astro`, `pages/geo/{index,russia,world,asia}.astro`, `pages/{about,editorial,advertising,contacts}.astro`, `content/columns/agentic-commerce-a16z-reklama.md`. Изменены: `components/{Header,Footer}.astro`, `styles/global.css`, `content/authors/y-sokolov.md`. Удалён: `content/reports/reklama-v-geoservisah-2026.md`.

## Цель сессии (Итерация 3, адаптированная — деплой, дизайн и SEO уже есть)

### 1. Контент (приоритет)
Заменить демо-материалы реальными по `CONTENT_TODO.md`: проверка фактов, реальные источники, правка фронт-маттера. Рабочий режим: владелец даёт тему/факты — сессия пишет и оформляет. Туда же — статичные тексты (профиль автора, «О нас»).

### 2. Приватный репозиторий + деплой Timeweb по SSH
Сделать репо `yuriysklv-hue/media` закрытым, не сломав автодеплой. **Порядок важен — сначала SSH, потом приватность:**
1. В панели Timeweb (приложение → Настройки → подключение репозитория) переключить способ доступа на **SSH**: Timeweb выдаёт/просит публичный SSH-ключ.
2. Добавить этот ключ в GitHub: репо → Settings → **Deploy keys** → Add deploy key (доступ read-only достаточен), URL репо в Timeweb — `git@github.com:yuriysklv-hue/media.git`.
3. Прогнать тестовый деплой (кнопка ▶) — убедиться, что клонирование по SSH работает **до** смены видимости.
4. GitHub → Settings → General → Danger Zone → **Change visibility → Private**.
5. Снова прогнать деплой и проверить автодеплой пушем в `main`.
Заметки: рабочие сессии Claude ходят в репо через GitHub App — приватность им не мешает; вебхук автодеплоя тоже сохраняется. Если у Timeweb «SSH-способ» выглядит иначе (например, только через их GitHub-интеграцию) — задокументировать фактический путь в CLAUDE.md.

### 3. Бургер-меню (мобильная навигация) — ✅ СДЕЛАНО (10.07.2026)
> Реализовано, см. блок «Уже закрыто (сессия 10.07.2026)». Исходное ТЗ ниже — для истории.

Кнопка-бургер в шапке сейчас декоративная (на <900px скрывается `.nav` и CTA, бургер ничего не открывает). Сделать открывающуюся панель. **Состав согласовать с владельцем на старте сессии**, предложение по умолчанию:
- пять рубрик (Новости / Дайджесты / Обзоры / Отчёты / Мнения) — как в десктоп-навигации;
- CTA «Подписаться»;
- опционально вторым блоком: гео (Россия / Мир / Азия — когда появятся гео-ленты) и «О нас».
Реализация: оверлей/выпадающая панель под шапкой в стиле макета (белый фон, линейки, sans), доступность (aria-expanded, закрытие по Esc и тапу вне), без JS-фреймворков — ванильный скрипт как в BaseLayout. Тот же пункт закрывает вопрос «куда ведут ссылки шапки на мобиле».

### 4. Служебные страницы — ✅ СДЕЛАНО (10.07.2026 + 09.07.2026)
- ✅ `/about` («О нас») — сделано;
- ✅ страница автора `/authors/y-sokolov` + schema (сделано 09.07);
- ✅ пункты футера «Реклама на сайте» (`/advertising`), «Редакция» (`/editorial`), «Контакты» (`/contacts`) — сделаны.

Остаточный хвост: заменить личный email на брендовый на `/contacts` и `/advertising`; оживить иконки TG/VK/RSS в футере, когда появятся соцканалы.

### 5. Качество
- Lighthouse (astro preview + playwright/lighthouse) — цель ≥90 Performance;
- Яндекс.Метрика — вставить счётчик, когда владелец даст номер;
- проверить фронт на реальном объёме контента (когда появятся длинные статьи);
- ✅ гео-ленты `/geo/russia|world|asia` + хаб `/geo` — сделано (10.07.2026), футер «Гео» провязан.

### 6. Подписка на рассылку (форма + размещение)
Сейчас блок подписки — **нерабочая заглушка** и стоит **непоследовательно**. Нужно довести до ума.

Текущее состояние:
- Форма (`.newsletter` + `.nl-form`) есть только на **главной** (`pages/index.astro`) и внизу **страницы материала** (`pages/article/[...slug].astro`). Обе — заглушки: `onsubmit="return false"`, email никуда не уходит.
- CTA «Подписаться» в шапке (`components/Header.astro`, десктоп + мобильное меню `.mm-cta`) — **без действия** вовсе.
- Формы **нет** на страницах рубрик (`SectionPage`), гео-лентах (`GeoPage`) и статических страницах (`PageLayout`) — а именно туда приходят из поиска/соцсетей.

Что сделать:
1. **Заставить форму работать.** Выбрать способ сбора email без своего бэкенда (сайт статический на Timeweb): внешний сервис форм/рассылки (напр. отправка на почту через сторонний endpoint, или провайдер рассылок с JS-виджетом/API). Согласовать с владельцем сервис и куда падают адреса. Учесть валидацию, состояние «спасибо/ошибка», защиту от спама, согласие на обработку ПДн (152-ФЗ) — чекбокс + ссылка на политику.
2. **CTA «Подписаться» в шапке** — привязать к форме: скролл к блоку на текущей странице либо к `/#subscribe` (добавить якорь у блока на главной), либо открывать модалку с формой. Один механизм на десктоп и бургер.
3. **Единый компонент и релевантное размещение.** Вынести блок в переиспользуемый компонент (`components/Newsletter.astro`) и показывать консистентно: точно на главной и материалах; решить по рубрикам/гео/статике — вероятно, компактный вариант в подвале контента, но не на служебных (Контакты/Реклама/О нас — там перегруз). Согласовать матрицу «где показываем».

Оговорка: провайдер и endpoint должен дать владелец (аккаунт сервиса, ключ/URL). До этого — задача в статусе «ждём решения по сервису».

### 7. SEO-задачи

> Консолидированный список открытых SEO-хвостов. Раньше они были размазаны по строке «Осталось из ТЗ 06» (выше) и разделу «5. Качество» — здесь единый источник. Полное ТЗ — `06_SEO_GEO_и_AI-разметка.md` (чек-листы: раздел 11 «на каждую публикацию», раздел 12 «при запуске сайта»).
>
> **Кодовый слой ТЗ 06 закрыт** (sitemap, robots.txt, `llms.txt`/`llms-full.txt`, RSS, JSON-LD-граф `NewsMediaOrganization`+`WebSite`+`NewsArticle`+`BreadcrumbList`+`CollectionPage`, OG-картинка, hreflang, meta robots) — см. блок «Уже закрыто из Итерации 3». Ниже — только то, что осталось.

**A. Сторона владельца (вне кода, нужны внешние аккаунты/данные):**
- [ ] **Яндекс.Вебмастер** — добавить сайт, подтвердить права, отправить `sitemap-index.xml`, следить за индексацией/ИКС.
- [ ] **Google Search Console** — добавить сайт, подтвердить, отправить sitemap. Индексацию в Google закрывает именно GSC, а не счётчик (Google Analytics не нужен).
- [ ] **Bing Webmaster Tools** — добавить сайт, отправить sitemap. Важно для LLM-видимости: на индекс Bing опираются веб-поиск ChatGPT и Perplexity (см. врезку «Как SEO связано с LLM-выдачей» ниже).
- [ ] **Яндекс.Метрика** — основной и единственный счётчик. Владелец даёт номер → вставляем в `BaseLayout`. Помимо аналитики даёт Яндексу «обход по счётчику Метрики» (ускоренная индексация) + поведенческие факторы. Plausible/Umami/GA не ставим.
- [ ] **Валидация разметки после деплоя** — прогнать материал через Rich Results Test, schema.org Validator; PageSpeed Insights ≥90.

**B. В коде (ждут внешних данных — сделать, когда данные появятся):**
- [ ] `sameAs` в `NewsMediaOrganization` + `twitter:site` — когда появятся соцканалы (TG/VK).
- [ ] 301-редиректы типовых опечаток (`/news/` → `/news`) и HSTS-заголовок (уровень Timeweb/Astro).

**C. Производительность (SEO-фактор, раздел 9 ТЗ 06):**
- [ ] Lighthouse / PageSpeed ≥90 Performance (astro preview + playwright). Дублируется с задачей «5. Качество» — ведём здесь.
- [ ] Проверить Core Web Vitals на реальном объёме контента (LCP <2.5s, INP <200ms, CLS <0.1).

**D. Расширения разметки из ТЗ 06, ещё не заведённые (кандидаты, по мере надобности):**
- [ ] `contentLocation` (`Place`/`City`) для материалов с гео-привязкой — раздел 6.3.
- [ ] `FAQPage` для материалов с Q&A-блоками — раздел 3.6.
- [ ] `Article` + `Dataset` для отчётов — раздел 3.3 (актуально с первым реальным отчётом; коллекция `reports` сейчас пуста).
- [ ] TDMRep (`/.well-known/tdmrep.json`) — раздел 7.5, опционально (когда появится платный/ограниченный контент).
- [ ] Метрики AI-цитируемости (рефереры от chatgpt.com / perplexity.ai / claude.ai) — раздел 10, план «Релиз 3».

**E. Разметка цитат (открытый вопрос — разобрать позже):**
- [ ] **Цитаты не размечены schema.org, хотя они в каждой второй новости.** Агентский пайплайн пишет прямые цитаты спикеров/источников, но в вёрстке нет `Quotation`/`citation` (или аналога) — LLM и поисковики не распознают их как цитаты первоисточника. Разобраться, где чинить: в шаблоне материала (`media-site`, `pages/article/[...slug].astro` + MDX-компоненты) или в самом пайплайне (`media-agents`, генерация фронт-маттера/тела). **Отложено до конца ревью SEO-раздела.**

<!-- Место для новых SEO-задач от владельца — впишем сюда после обсуждения. -->

---

### Врезка: как SEO-задачи связаны с выдачей в LLM-чатах

Чтобы попадать в ответы ChatGPT, Perplexity, Gemini, YandexGPT и т.п., работает **не отдельная «магия для ИИ», а обычная индексация + структура под цитирование.** Два канала:

1. **Обучающий корпус** (боты `GPTBot`/`ClaudeBot`/`CCBot` из `robots.txt`) — модель обучается на скрейпе с лагом в месяцы; для нового сайта в моменте почти не работает. У нас пущены «на вырост».
2. **Живой веб-поиск в момент ответа (RAG)** — главный канал для нового сайта: чат ищет по вебу, читает верхние страницы, цитирует со ссылкой. Почти все грундятся на существующих поисковых индексах:
   - ChatGPT Search, Perplexity, Copilot → **Bing** (+ свои краулеры);
   - Gemini / AI Overviews → **Google**;
   - YandexGPT / Нейро → **Яндекс**.

**Практический вывод:** индексация в Google + **Bing** + Яндексе = фундамент LLM-видимости (отсюда обязательный Bing Webmaster Tools в задаче A). `llms.txt`/`llms-full.txt` — молодой стандарт, поддержка у движков слабая, держим «на вырост». Чистая структура (H1→H2, первый абзац = саммари, факты с источниками, размеченные цитаты) → LLM легче вытащить готовую цитату именно из нас.

## Подводные камни

- Канон дизайна — `mockups/1screen-mockup-minimal.html` и CLAUDE.md (НЕ ТЗ 02/09: там serif и старая палитра).
- Всё в `media-site/`; сборка `cd media-site && npm run build`. Рабочая ветка сессии ≠ `main`: деплой случится только после мержа PR в `main` (делает владелец).
- Playwright — глобально: `import pkg from '/opt/node22/lib/node_modules/playwright/index.js'; const { chromium } = pkg;`.
- `@astrojs/mdx` держать на `^4` (7.x требует Astro 7).
- Даты контента: `pubDate` указывать с `+03:00`; хелперы формата уже считают всё по Europe/Moscow.
- **Деплой Timeweb иногда падает по их вине** (`i/o timeout`, `Could not resolve host: github.com` в логе) — код ни при чём, лечится кнопкой ▶ (перезапуск деплоя) через несколько минут. Проверено 06.07.2026.
- Приватность репо менять только **после** проверенного SSH-доступа Timeweb (см. задачу 2), иначе автодеплой сломается.

## Критерий приёмки

- Build чистый (кроме безобидного `WARN … "reports" is empty` — см. выше);
- репо приватный, автодеплой Timeweb работает по SSH (проверено пушем в `main`) — **осталось**;
- ✅ бургер открывает мобильное меню на <900px;
- реальные материалы проходят схему коллекций и корректно выглядят на главной, в рубриках и на странице материала.
